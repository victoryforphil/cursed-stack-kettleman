language: "typescript"
type: 'application'

project:
  name: 'cursed-server'
  description: 'Server for cursed'
  metadata:
    tag: 'cursed-server'

tags:
- 'prisma'
- 'docker'

tasks:
  entrypoint:
    command: './scripts/entrypoint.sh'
    toolchain: 'system'
    deps:
    - '~:install'
